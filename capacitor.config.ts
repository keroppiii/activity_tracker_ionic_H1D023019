import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'activity_tracker_ionic_H1D023019',
  webDir: 'dist'
};

export default config;
